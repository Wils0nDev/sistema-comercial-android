<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".UI.principal">

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/menu_principal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_gravity="bottom"
        android:layout_marginBottom="0dp"
        android:background="@color/colorPrimary"
        app:itemIconTint="@drawable/bottom_navigation_principal_colors"
        app:itemTextColor="@drawable/bottom_navigation_principal_colors"
        app:menu="@menu/menu_bottom_bar" />

    <FrameLayout
        android:id="@+id/frame_principal"
        android:layout_width="match_parent"
        android:layout_above="@id/menu_principal"
        android:layout_height="match_parent">

    </FrameLayout>

</RelativeLayout>